/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["www.hubspot.com", "islegeeks.com", "vocalvideo.com"],
  },
  reactStrictMode: true,
};

export default nextConfig;
